[package]
name = "token"
version = "0.1.0"
authors = ["Portaldot Dev"]
edition = "2021"

[dependencies]
ink = { version = "=5.0.2", default-features = false }

[lib]
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
]
ink-as-dependency = []
